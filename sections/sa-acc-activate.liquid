<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script><script src="/apps/sa/check_plugin.js?shop=skywalltv.myshopify.com"></script>
			<script>
			if(typeof plugin_logged_in != "undefined" && plugin_logged_in==1)
            {
			$(document).ready(function(){
				$("input[value='activate_customer_password']").parents("form").addClass("activate_customer_password");
				$(".activate_customer_password :submit").click(function(event){
					event.preventDefault();
					$.ajax({ 
					url:"/apps/sa/customers.php?type=activate_account",
					type: "POST",
					dataType: "json",
					data:$(".activate_customer_password").serialize(),
					success:function(data){
						$(".activate_customer_password").submit();
					},
						error: function (test) {
						}
					});
				});
				});
			}
			</script>